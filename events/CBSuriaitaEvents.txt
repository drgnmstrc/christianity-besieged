namespace = CBSuriaitaEvents

country_event = {# A Final Choice
	id = CBSuriaitaEvents.0
	title = CBSuriaitaEvents.0t
	desc = CBSuriaitaEvents.0d
	picture = AULIC_COUNCIL_eventPicture
	fire_only_once = yes
	is_triggered_only = yes
	immediate = {
		complete_mission = SRY_kingly_favors
		complete_mission = SRY_greasing_wheels
		complete_mission = SRY_ceremonial_preparations
		complete_mission = SRY_untenable_situation
		complete_mission = SRY_scoping_opponents
		complete_mission = SRY_holy_communion
	}
	option = { # Let us hope our diplomatic efforts paid off
		name = CBSuriaitaEvents.0a
		CLC = {
			country_event = {
				id = CBSuriaitaEvents.1
				days = 10
			}
		}
		ai_chance = {
			factor = 1
			modifier = {
				factor = 2
				has_country_flag = kingly_favors
			}
			modifier = {
				factor = 2
				has_country_flag = greasing_wheels
			}
			modifier = {
				factor = 2
				has_country_flag = ceremonial_preparations
			}
		}
	}
	option = { #Time to see just how threatening we can be...
		name = CBSuriaitaEvents.0b
		CLC = {
			country_event = {
				id = CBSuriaitaEvents.4
				days = 10
			}
		}
		ai_chance = {
			factor = 1
			modifier = {
				factor = 2
				has_country_flag = untenable_situation_completed
			}
			modifier = {
				factor = 2
				has_country_flag = scoping_opponents_completed
			}
			modifier = {
				factor = 2
				has_country_flag = holy_communion_completed
			}
		}
	}
}

country_event = {# An Audience with the Patriarch
	id = CBSuriaitaEvents.1
	title = CBSuriaitaEvents.1t
	desc = CBSuriaitaEvents.1d
	picture = AULIC_COUNCIL_eventPicture
	fire_only_once = yes
	is_triggered_only = yes
	option = { # Not once have I said you could proselytize, even less so to me!
		name = CBSuriaitaEvents.1a
		ai_chance = {
			factor = 2
		}
		SRY = {
			country_event = {
				id = CBSuriaitaEvents.2
				days = 10
			}
		}
	}
	option = { # I humble myself before the holy father. 
		name = CBSuriaitaEvents.1b
		change_religion = syriac
		SRY = { grant_independence = yes }
		every_province = {
			limit = { area = aleppo_area }
			add_core = SRY
			remove_core = ROOT
			cede_province = SRY
		}
		SRY = {
			create_subject = {
				subject_type = syriac_brotherhood
				subject = ROOT
			}
		}
		change_government = theocracy
		add_government_reform = syriac_akhuta_reform
		SRY = {
			country_event = {
				id = CBSuriaitaEvents.3
				days = 10
			}
		}
		ai_chance = {
			factor = 1
			modifier = {
				factor = 2
				SRY = { has_country_flag = kingly_favors_completed }
			}
			modifier = {
				factor = 2
				SRY = { has_country_flag = greasing_wheels_completed }
			}
			modifier = {
				factor = 2
				SRY = { has_country_flag = ceremonial_preparations_completed }
			}
		}
	}
}

country_event = {# Cilician [CLC.Monarch.GetTitle] Refuses Conversion!
	id = CBSuriaitaEvents.2
	title = CBSuriaitaEvents.2t
	desc = CBSuriaitaEvents.2d
	picture = AULIC_COUNCIL_eventPicture
	fire_only_once = yes
	is_triggered_only = yes
	option = { # Then war it is!
		name = CBSuriaitaEvents.2a
		FROM = { release = SRY }
		declare_war_with_cb = {
			casus_belli = cb_force_communion
			who = CLC
		}
	}
}

country_event = {# Cilician [CLC.Monarch.GetTitle] Accepts Conversion!
	id = CBSuriaitaEvents.3
	title = CBSuriaitaEvents.3t
	desc = CBSuriaitaEvents.3d
	picture = AULIC_COUNCIL_eventPicture
	fire_only_once = yes
	is_triggered_only = yes
	option = { # What a joyous day!
		name = CBSuriaitaEvents.3a
	}
}

country_event = {# Patriarchal Envoy Threatens the [Root.Monarch.GetTitle]
	id = CBSuriaitaEvents.4
	title = CBSuriaitaEvents.4t
	desc = CBSuriaitaEvents.4d
	picture = AULIC_COUNCIL_eventPicture
	fire_only_once = yes
	is_triggered_only = yes
	option = { # You think you can threaten ME?!
		name = CBSuriaitaEvents.4a
		ai_chance = {
			factor = 2
		}
		SRY = {
			country_event = {
				id = CBSuriaitaEvents.5
				days = 10
			}
		}
	}
	option = { # I suppose we haven't much choice... 
		name = CBSuriaitaEvents.4b
		change_religion = syriac
		SRY = { grant_independence = yes }
		every_province = {
			limit = { area = aleppo_area }
			add_core = SRY
			remove_core = ROOT
			cede_province = SRY
		}
		SRY = {
			create_subject = {
				subject_type = syriac_brotherhood
				subject = ROOT
			}
		}
		change_government = theocracy
		add_government_reform = syriac_akhuta_reform
		SRY = {
			country_event = {
				id = CBSuriaitaEvents.6
				days = 10
			}
		}
		ai_chance = {
			factor = 1
			modifier = {
				factor = 2
				SRY = { has_country_flag = untenable_situation_completed }
			}
			modifier = {
				factor = 2
				SRY = { has_country_flag = scoping_opponents_completed }
			}
			modifier = {
				factor = 2
				SRY = { has_country_flag = holy_communion_completed }
			}
		}
	}
}

country_event = {# Cilician [CLC.Monarch.GetTitle] Refuses Our Ultimatum!
	id = CBSuriaitaEvents.5
	title = CBSuriaitaEvents.5t
	desc = CBSuriaitaEvents.5d
	picture = AULIC_COUNCIL_eventPicture
	fire_only_once = yes
	is_triggered_only = yes
	option = { # Then war it is!
		name = CBSuriaitaEvents.5a
		FROM = { release = SRY }
		declare_war_with_cb = {
			casus_belli = cb_force_communion
			who = CLC
		}
	}
}

country_event = {# Cilician [CLC.Monarch.GetTitle] Accepts Our Ultimatum!
	id = CBSuriaitaEvents.6
	title = CBSuriaitaEvents.6t
	desc = CBSuriaitaEvents.6d
	picture = AULIC_COUNCIL_eventPicture
	fire_only_once = yes
	is_triggered_only = yes
	option = { # What a joyous day!
		name = CBSuriaitaEvents.6a
	}
}
